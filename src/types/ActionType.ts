export enum ActionType {
    DELETE,
    DELETED,
    PIN,
    PINNED,
    UNPIN,
    UNPINNED,
    CREATE_FILE,
    CREATED_FILE,
    CREATE_DIRECTORY,
    CREATED_DIRECTORY,
    IMPORT,
    IMPORTED,
    EXPORT,
    EXPORTED,
}
